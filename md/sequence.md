````mermaid
sequenceDiagram
    participant 定时任务调度器
    participant 业务逻辑服务
    participant 员工信息数据库
    participant Coze工作流
    participant 邮件发送服务
    定时任务调度器 ->> 业务逻辑服务: 每日定时触发
    业务逻辑服务 ->> 业务逻辑服务: 执行日期判断 
    业务逻辑服务 ->> 员工信息数据库: 查询员工信息 (姓名/邮箱)
    员工信息数据库 -->> 业务逻辑服务: 返回员工数据集
    loop 遍历获取的员工
        业务逻辑服务 ->> Coze工作流: 异步请求祝福内容生成 (员工姓名, 节假日主题)
        Coze工作流 -->> 邮件发送服务: 异步发送节假日祝福邮件 (收件人, 内容)
%%        邮件发送服务 -->> 业务逻辑服务: 确认邮件发送状态
    end

    业务逻辑服务 ->> 定时任务调度器: 完成当前周期处理
````